server:
  port: ${SERVER_PORT:8097}
  servlet:
    context-path: /cn

spring:
  application:
    name: BM-CORE 
     
eureka:
  client:
    service-url:
      defaultZone: http://service-registry:8761/eureka/
